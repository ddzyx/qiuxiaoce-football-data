---
name: qiuxiaoce-football-data
version: 2.4.3
description: 球小策（qiuxiaoce.com）足球结构化数据 Skill。当用户要查询足球比赛赛程、单场赛前数据（Match-Pack）、当日赛事摘要、已发布研报、市场参考值变化、历史样本复盘、API 额度，或基于这些数据创作自媒体内容（短视频脚本、公众号文章、社群简报、定制分析提示词）时使用。触发词：查比赛、今天比赛、明天赛程、赛事前瞻、比赛数据、Match-Pack、今天有什么比赛、今日研报、历史命中、复盘、赔率变化、市场变化、写公众号、做短视频脚本、赛事简报、定制提示词、查额度、更新 Skill。不用于：投注建议或结果保证（任何表述形式）、第三方软件安装教学、与球小策数据服务无关的通用足球话题闲聊。
---

# 球小策足球结构化数据 Skill

本文件是**路由入口**：识别用户意图后，加载 `workflows/` 下对应的子流程执行，不要凭本文件直接作答数据类问题。

## 1. 定位与边界

将球小策作为足球结构化数据服务使用。调用 API 获取赛事事实、衍生特征、历史公开研报和赛后结果，再由当前模型按用户要求完成整理、比较、写作或历史样本评估。

遵守以下边界：

- 不提供投注建议、下注指令、所谓稳胆或结果保证。
- 不把预测首发、伤停影响评分、市场变化或模型输出表述为确定事实。
- 不声称代表球小策官方推荐；已发布研报仅作为来源之一。
- 不负责第三方工具的免费安装教学。涉及第三方软件安装、目录、权限和兼容性时，引导用户查阅相应官方文档。
- 不泄露完整 API Key，不在回复、日志、命令示例或生成内容中回显密钥。

## 2. 意图识别与路由（核心）

### 第一步：解析用户输入

从自然语言中提取结构化参数，**用你的足球知识自行归一化，默认不追问**：

- **球队名**：用户可能给简称、绰号、英文名或错别字（如"红魔""枪手""皇马""国米""PSG"），映射到官方中文名；归一化细节见 `workflows/match_query.md`。
- **日期**：用户未给日期默认北京时间今天；"明天 / 今晚 / 这周五"换算成具体 `YYYY-MM-DD`。
- **交付物**：先判断用户最终要什么——① 只要数据事实 ② 要解读分析 ③ 要可发布成稿。这决定加载哪个子流程以及是否继续加载模板。

### 第二步：按意图路由

| 用户意图 | 典型输入 | 必读文件 |
|:---|:---|:---|
| 查某场比赛数据 / 前瞻 | "皇马这场怎么看"、"明天曼城比赛数据" | `workflows/match_query.md` |
| 当日赛事总览 / 已发布研报 | "今天有什么比赛"、"今天的研报" | `workflows/today_content.md` |
| 历史复盘 / 样本表现 | "最近准不准"、"历史命中怎么样" | `workflows/backtest.md` |
| 市场参考值变化 | "这场赔率怎么变的" | `workflows/market.md` |
| 内容创作（文章/脚本/简报/提示词） | "写一篇公众号"、"做个短视频脚本" | `workflows/content_creation.md` |
| 查询额度 | "还剩多少点数" | 直接运行 `scripts/check_quota.py` |
| 更新 Skill | "检查更新"、"更新 Skill" | `workflows/updating.md` |

### 第三步：加载子流程执行

- 一次只加载路由命中的子流程文件，不要预加载全部；
- 子流程内部会指引按需加载模板或契约文件；
- **仅在无法唯一确定用户意图时才追问**（如"今晚那场德比"而当夜有多场德比），且一次问全所需信息；
- 同一支球队同一天只有一场比赛——定位类场景永远不要让用户确认候选项。

## 3. 公共约定（所有子流程通用）

### 认证

- 优先从环境变量 `QIUXIAOCE_API_KEY` 读取密钥；
- 密钥缺失时，提示用户前往 `https://www.qiuxiaoce.com/data-account/` 领取或查看；不要求用户在公开对话中粘贴完整密钥；
- 仅在用户明确同意后持久化密钥，优先使用宿主工具的安全凭据存储，不默认写入项目目录；
- 仅在用户明确要求时使用脚本 `--key` 临时参数，并提醒该参数可能进入命令历史或进程列表。

### 直接 HTTP

Base URL：`https://www.qiuxiaoce.com/wp-json/abv2-creator/v1`
认证请求头：`X-API-Key: <用户密钥>`

详细端点、字段与点数以 `references/api_schema.json` 和官网接口文档当前版本为准。

### 输出规范

- 区分事实、预测字段、衍生评分和模型判断；
- 标注数据日期、比赛时间及时区；默认面向中文用户使用北京时间，并保留接口原始时间字段供核对；
- 只使用接口实际返回的数据，缺失字段注明"当前接口未返回"，不得补造；
- 不输出隐藏思维链；可以简要说明使用了哪些数据类别、有哪些缺失和结论依据；
- 不因用户要求"必须给明确结果"而编造确定性结论；
- 调用付费接口前可按需运行 `scripts/check_quota.py` 并向用户说明预计使用的接口，不做无意义重复请求；
- 对外发布内容附上：

> 数据来源：球小策足球数据服务（qiuxiaoce.com）。预测字段与模型分析仅供赛事信息研究，不代表确定结果。

## 4. 资源索引

按任务需要加载，不要一次性加载全部资源。

### 子流程（workflows/）

- 单场比赛查询：`workflows/match_query.md`
- 当日赛事与研报：`workflows/today_content.md`
- 历史样本评估：`workflows/backtest.md`
- 市场变化数据：`workflows/market.md`
- 内容创作：`workflows/content_creation.md`
- Skill 更新：`workflows/updating.md`

### 脚本（scripts/）

- `scripts/fetch_match.py`：赛程候选定位与单场 Match-Pack 查询
- `scripts/query_backtest.py`：历史公开研报与赛后结果检索
- `scripts/check_quota.py`：API Key 额度自查
- `scripts/update_skill.py`：Skill 安全更新与冲突处理

### 模板与契约

- 内容模板：`templates/`（由 `workflows/content_creation.md` 路由加载）
- 分析提示词定制：`prompts/prompt_optimizer.md`
- API 契约速查：`references/api_schema.json`
